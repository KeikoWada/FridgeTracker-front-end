<ul>
{{#each lists as |expiration_date|}}
<li id='data' data-id="{{expiration_date.id}}">Expiration Date: {{expiration_date.item}}
  <button id="add" class="btn btn-default">add item</button>
  <button id="remove" class="btn btn-default">remove item</button>
  <!-- {{> partial}} -->


 <!-- Developers, why are we using data-id and not just id? -->

  <div>
    {{#if expiration_date.date}}
      {{expiration_date.date}}
      {{expiration_date.item_name}}
      {{expiration_date.category}}
    {{else}}
      Nothing to waste! Great job!
    {{/if}}
  </div>
</li>
{{/each}}
</ul>
